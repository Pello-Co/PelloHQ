---
import Layout from '../layouts/Layout.astro';
import Header from '../components/layout/Header.astro';
import Footer from '../components/layout/Footer.astro';
import HeroPello from '../components/sections/HeroPello.astro';
import HowItWorks from '../components/sections/HowItWorks.astro';
import ProductSection from '../components/sections/ProductSection.astro';
import UseCases from '../components/sections/UseCases.astro';
import Stats from '../components/sections/Stats.astro';
import FAQ from '../components/sections/FAQ.astro';
import FinalCTA from '../components/sections/FinalCTA.astro';
---

<Layout title="Pello — AI-Powered Full Stack Marketing Agency" description="Pello propels business growth with AI-powered marketing across SEO, outreach, content, social, ads, and custom automations. Enter your website to see what Pello can do for you.">
  <Header />
  <main id="main-content">
    <div id="hero-wrap" class="relative">
      <canvas class="bg-canvas" aria-hidden="true" style="position:absolute;inset:0;z-index:0;pointer-events:none;will-change:transform;opacity:0;transition:opacity 2s ease-in;"></canvas>
      <HeroPello />
    </div>

    <HowItWorks />

    <div id="products">
      <ProductSection productIndex={0} flip={false} />
      <ProductSection productIndex={1} flip={true} />
      <ProductSection productIndex={2} flip={false} />
      <ProductSection productIndex={3} flip={true} />
      <ProductSection productIndex={4} flip={false} />
      <ProductSection productIndex={5} flip={true} />
      <ProductSection productIndex={6} flip={false} />
      <ProductSection productIndex={7} flip={true} />
    </div>

    <UseCases />
    <Stats />
    <FAQ />
    <FinalCTA />
  </main>
  <Footer />
</Layout>

<a id="skip-link" href="#main-content">Skip to main content</a>

<style is:global>
  #skip-link {
    position: absolute;
    top: -100%;
    left: 1rem;
    z-index: 9999;
    padding: 0.75rem 1.25rem;
    background: var(--color-brand-primary);
    color: white;
    font-weight: 600;
    border-radius: 0 0 0.5rem 0.5rem;
    text-decoration: none;
    transition: top 0.1s;
  }
  #skip-link:focus { top: 0; }
  #hero-wrap .url-input-wrap,
  .final-cta .url-input-wrap {
    background: rgba(255,255,255,0.06) !important;
    backdrop-filter: blur(24px) saturate(1.4);
    -webkit-backdrop-filter: blur(24px) saturate(1.4);
    border: 1px solid rgba(255,255,255,0.12) !important;
    box-shadow: 0 4px 30px rgba(0,0,0,0.2), inset 0 1px 0 rgba(255,255,255,0.05);
  }
  #hero-wrap .hero-panel {
    background: rgba(6,7,18,0.55) !important;
    backdrop-filter: blur(20px) saturate(1.3);
    -webkit-backdrop-filter: blur(20px) saturate(1.3);
    border: 1px solid rgba(255,255,255,0.12) !important;
  }
  #hero-wrap .panel-source-row { border-bottom: 1px solid rgba(255,255,255,0.1) !important; }
  #hero-wrap .core-inner {
    background: rgba(0,0,0,0.15) !important;
    border: 1px solid rgba(255,255,255,0.1) !important;
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
  }
  #hero-wrap .panel-core-section { border-bottom: 1px solid rgba(255,255,255,0.1) !important; }
  #hero-wrap .channel-node {
    background: rgba(255,255,255,0.03) !important;
    border: 1px solid rgba(255,255,255,0.1) !important;
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
  }
  #hero-wrap .panel-header-bar { border-bottom: 1px solid rgba(255,255,255,0.1) !important; }
  #hero-wrap .panel-ready-bar { border-top: 1px solid rgba(255,255,255,0.1) !important; }
  #hero-wrap .node-stat-text { color: #a78bfa !important; }
  #hero-wrap .hero-panel-mobile {
    background: rgba(10,10,20,0.3);
    backdrop-filter: blur(20px) saturate(1.3);
    -webkit-backdrop-filter: blur(20px) saturate(1.3);
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 1.25rem;
    padding: 1rem;
    box-shadow: 0 8px 32px rgba(0,0,0,0.3);
  }
  .final-cta {
    background: rgba(20,15,40,0.35) !important;
    backdrop-filter: blur(24px) saturate(1.4) !important;
    -webkit-backdrop-filter: blur(24px) saturate(1.4) !important;
    border-color: rgba(99,102,241,0.15) !important;
    box-shadow: 0 8px 40px rgba(0,0,0,0.3), inset 0 1px 0 rgba(255,255,255,0.04) !important;
    overflow: hidden;
    position: relative;
  }
  .final-cta > .absolute.inset-0.pointer-events-none { display: none !important; }
  #hero-wrap .grid-texture,
  #hero-wrap .absolute.inset-0.pointer-events-none[style*="radial-gradient"] { display: none !important; }
  #hero { background: transparent !important; }
</style>

<script>
  const ric = (window as any).requestIdleCallback
    ? (window as any).requestIdleCallback
    : (cb: () => void) => setTimeout(cb, 1);

  ric(() => {

  function valueNoise(x: number): number {
    const i = Math.floor(x);
    const f = x - i;
    const u = f * f * (3 - 2 * f);
    const a = Math.sin(i * 127.1) * 43758.5453;
    const b = Math.sin((i + 1) * 127.1) * 43758.5453;
    return (a - Math.floor(a)) * (1 - u) + (b - Math.floor(b)) * u;
  }

  // Neon phosphor variants of product colors
  const COLORS: string[] = [
    '#818cf8','#a78bfa','#c084fc','#e879f9',
    '#a855f7','#6366f1','#c026d3','#7c3aed',
  ];
  const NAMES: string[] = ['PELLOSEO','PELLOREACH','PELLOPUBLISH','PELLOSOCIAL','PELLOPITCH','PELLOADS','PELLOFLOW','PELLOBUILD'];

  interface OscWave {
    yFrac: number;
    freq: number;
    baseAmp: number;
    phase: number;
    phaseSpeed: number;
    noiseOffset: number;
    noisePeriod: number;
    color: string;
    name: string;
  }

  const WAVES: OscWave[] = COLORS.map((color, i) => ({
    yFrac: 0.1 + (i / 7) * 0.8,
    freq: 0.009 + i * 0.002,
    baseAmp: 10 + Math.sin(i * 1.5) * 4,
    phase: i * 0.7,
    phaseSpeed: 0.007 + i * 0.001,
    noiseOffset: i * 83.1,
    noisePeriod: 3 + i * 0.3,
    color,
    name: NAMES[i],
  }));

  let mouseX: number | null = null;
  let mouseY: number | null = null;

  function setupCanvas(canvas: HTMLCanvasElement, wrap: HTMLElement, waves: OscWave[], isSimple: boolean): () => void {
    const ctx = canvas.getContext('2d')!;
    let W = 0, H = 0;
    let dpr = window.devicePixelRatio || 1;

    function resize() {
      dpr = window.devicePixelRatio || 1;
      const rect = wrap.getBoundingClientRect();
      W = Math.round(rect.width);
      H = Math.round(rect.height);
      canvas.width = W * dpr;
      canvas.height = H * dpr;
      canvas.style.width = W + 'px';
      canvas.style.height = H + 'px';
      ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
    }

    const ro = new ResizeObserver(() => resize());
    ro.observe(wrap);
    resize();

    const mobile = () => W < 768;
    let triggerX = 0; // sweep position

    function draw() {
      requestAnimationFrame(draw);
      if (W === 0 || H === 0) return;
      ctx.clearRect(0, 0, W, H);

      const now = performance.now() * 0.001;
      const SAMPLES = mobile() ? 100 : 200;

      // CRT grid
      if (!isSimple) {
        ctx.strokeStyle = 'rgba(100,120,160,0.04)';
        ctx.lineWidth = 1;
        const gridStep = 50;
        for (let gx = 0; gx < W; gx += gridStep) {
          ctx.beginPath(); ctx.moveTo(gx, 0); ctx.lineTo(gx, H); ctx.stroke();
        }
        for (let gy = 0; gy < H; gy += gridStep) {
          ctx.beginPath(); ctx.moveTo(0, gy); ctx.lineTo(W, gy); ctx.stroke();
        }

        // Time axis labels
        ctx.font = '8px monospace';
        ctx.fillStyle = 'rgba(150,180,220,0.25)';
        ctx.textAlign = 'center';
        for (let t2 = 0; t2 <= 5; t2++) {
          ctx.fillText(t2 * 100 + 'ms', (t2 / 5) * W, H - 4);
        }
        ctx.textAlign = 'left';

        // Trigger sweep line (every 3s)
        const triggerPeriod = 3;
        triggerX = ((now % triggerPeriod) / triggerPeriod) * W;
        ctx.beginPath();
        ctx.moveTo(triggerX, 0);
        ctx.lineTo(triggerX, H);
        ctx.strokeStyle = 'rgba(200,200,255,0.25)';
        ctx.lineWidth = 1;
        ctx.stroke();
      }

      // Find nearest wave for readout
      let nearestWaveIdx = -1;
      let nearestDist = Infinity;
      if (!isSimple && mouseY !== null) {
        for (let i = 0; i < waves.length; i++) {
          const d = Math.abs(mouseY - waves[i].yFrac * H);
          if (d < nearestDist) { nearestDist = d; nearestWaveIdx = i; }
        }
        if (nearestDist > H * 0.15) nearestWaveIdx = -1;
      }

      // Draw waves with phosphor glow
      for (let wi = 0; wi < waves.length; wi++) {
        const w = waves[wi];
        const baseY = w.yFrac * H;
        const noiseVal = valueNoise(now / w.noisePeriod + w.noiseOffset);
        const amp = w.baseAmp * (0.6 + noiseVal * 0.8);

        const pts: {x: number, y: number}[] = [];
        for (let s = 0; s <= SAMPLES; s++) {
          const x = (s / SAMPLES) * W;
          const y = baseY + Math.sin(x * w.freq + w.phase) * amp;
          pts.push({x, y});
        }

        const isNear = wi === nearestWaveIdx;
        const scanY = ((now % 8) / 8) * H;
        const scanBright = Math.max(0, 1 - Math.abs(baseY - scanY) / 40);

        // Wide outer bloom
        ctx.beginPath();
        pts.forEach((p, idx) => idx === 0 ? ctx.moveTo(p.x, p.y) : ctx.lineTo(p.x, p.y));
        ctx.strokeStyle = w.color;
        ctx.globalAlpha = isNear ? 0.12 : 0.04;
        ctx.lineWidth = 6;
        ctx.stroke();

        // Mid glow
        ctx.beginPath();
        pts.forEach((p, idx) => idx === 0 ? ctx.moveTo(p.x, p.y) : ctx.lineTo(p.x, p.y));
        ctx.globalAlpha = isNear ? 0.25 : (0.08 + scanBright * 0.1);
        ctx.lineWidth = 3;
        ctx.stroke();

        // Crisp center
        ctx.beginPath();
        pts.forEach((p, idx) => idx === 0 ? ctx.moveTo(p.x, p.y) : ctx.lineTo(p.x, p.y));
        ctx.globalAlpha = isNear ? 0.9 : (0.6 + scanBright * 0.2);
        ctx.lineWidth = 1;
        ctx.stroke();
        ctx.globalAlpha = 1;
      }

      // Crosshair + readout near mouse
      if (!isSimple && mouseX !== null && mouseY !== null && nearestWaveIdx >= 0) {
        const w = waves[nearestWaveIdx];
        // Crosshair
        ctx.setLineDash([4, 4]);
        ctx.strokeStyle = 'rgba(200,200,255,0.2)';
        ctx.lineWidth = 1;
        ctx.beginPath(); ctx.moveTo(mouseX, 0); ctx.lineTo(mouseX, H); ctx.stroke();
        ctx.beginPath(); ctx.moveTo(0, mouseY); ctx.lineTo(W, mouseY); ctx.stroke();
        ctx.setLineDash([]);

        // Readout
        const freq = (w.freq * 100).toFixed(1);
        const noiseVal = valueNoise(performance.now() * 0.001 / w.noisePeriod + w.noiseOffset);
        const amp = (w.baseAmp * (0.6 + noiseVal * 0.8)).toFixed(0);
        const readout = 'f: ' + freq + 'Hz  A: ' + amp + 'px';
        ctx.font = '10px monospace';
        ctx.fillStyle = w.color;
        ctx.globalAlpha = 0.8;
        ctx.fillText(readout, Math.min(mouseX + 8, W - 140), mouseY - 6);
        ctx.globalAlpha = 1;
      }

      // Scan line phosphor band
      if (!isSimple) {
        const scanY = ((now % 8) / 8) * H;
        const sg = ctx.createLinearGradient(0, scanY - 15, 0, scanY + 15);
        sg.addColorStop(0, 'rgba(120,180,255,0)');
        sg.addColorStop(0.5, 'rgba(120,180,255,0.04)');
        sg.addColorStop(1, 'rgba(120,180,255,0)');
        ctx.fillStyle = sg;
        ctx.fillRect(0, scanY - 15, W, 30);
      }

      // CRT vignette
      if (!isSimple) {
        const vg = ctx.createRadialGradient(W/2, H/2, H*0.3, W/2, H/2, H*0.8);
        vg.addColorStop(0, 'rgba(0,0,0,0)');
        vg.addColorStop(1, 'rgba(0,0,0,0.35)');
        ctx.fillStyle = vg;
        ctx.fillRect(0, 0, W, H);
      }

      for (const w of waves) w.phase += w.phaseSpeed;
    }

    requestAnimationFrame(draw);
    requestAnimationFrame(() => { canvas.style.opacity = '1'; });
    return () => ro.disconnect();
  }

  const heroWrap = document.getElementById('hero-wrap')!;
  const heroCanvas = heroWrap.querySelector<HTMLCanvasElement>('.bg-canvas')!;
  setupCanvas(heroCanvas, heroWrap, WAVES, false);

  heroWrap.addEventListener('mousemove', (e: MouseEvent) => {
    const rect = heroWrap.getBoundingClientRect();
    mouseX = e.clientX - rect.left;
    mouseY = e.clientY - rect.top;
  });
  heroWrap.addEventListener('mouseleave', () => { mouseX = null; mouseY = null; });

  const gridTex = heroWrap.querySelector<HTMLElement>('.grid-texture');
  if (gridTex) gridTex.style.display = 'none';

  const ctaCard = document.querySelector<HTMLElement>('.final-cta');
  if (ctaCard) {
    const ctaCanvas = document.createElement('canvas');
    ctaCanvas.setAttribute('aria-hidden', 'true');
    ctaCanvas.style.cssText = 'position:absolute;inset:0;z-index:0;pointer-events:none;border-radius:inherit;opacity:0;transition:opacity 2s ease-in;';
    ctaCard.style.position = 'relative';
    ctaCard.insertBefore(ctaCanvas, ctaCard.firstChild);
    const ctaWaves: OscWave[] = [
      { yFrac: 0.3, freq: 0.012, baseAmp: 9, phase: 0, phaseSpeed: 0.008, noiseOffset: 200, noisePeriod: 4, color: '#818cf8', name: 'PELLOSEO' },
      { yFrac: 0.5, freq: 0.016, baseAmp: 11, phase: 1.5, phaseSpeed: 0.01, noiseOffset: 350, noisePeriod: 3.5, color: '#c084fc', name: 'PELLOPUBLISH' },
      { yFrac: 0.7, freq: 0.01, baseAmp: 8, phase: 3.0, phaseSpeed: 0.007, noiseOffset: 500, noisePeriod: 5, color: '#a855f7', name: 'PELLOFLOW' },
    ];
    setupCanvas(ctaCanvas, ctaCard, ctaWaves, true);
    const gt = ctaCard.querySelector<HTMLElement>('.grid-texture');
    if (gt) gt.style.display = 'none';
  }

  }); // end ric
</script>
