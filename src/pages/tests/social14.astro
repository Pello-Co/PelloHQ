---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/layout/Header.astro';
import Footer from '../../components/layout/Footer.astro';
import HeroPello from '../../components/sections/HeroPello.astro';
import HowItWorks from '../../components/sections/HowItWorks.astro';
import ProductSection from '../../components/sections/ProductSection.astro';
import UseCases from '../../components/sections/UseCases.astro';
import Stats from '../../components/sections/Stats.astro';
import FAQ from '../../components/sections/FAQ.astro';
import FinalCTA from '../../components/sections/FinalCTA.astro';
---

<Layout title="PelloSocial — Neon Globe: Activity Rings | Pello" description="PelloSocial phone farm globe visualization — Orbiting activity rings representing platform posts in real time.">
  <Header />
  <main id="main-content">
    <div id="hero-wrap" class="relative">
      <canvas class="globe-canvas" aria-hidden="true" style="position:absolute;inset:0;z-index:1;pointer-events:none;will-change:transform;opacity:0;transition:opacity 1.5s ease-in;"></canvas>
      <HeroPello />
    </div>
    <HowItWorks />
    <div id="products">
      <ProductSection productIndex={0} flip={false} />
      <ProductSection productIndex={1} flip={true} />
      <ProductSection productIndex={2} flip={false} />
      <ProductSection productIndex={3} flip={true} />
      <ProductSection productIndex={4} flip={false} />
      <ProductSection productIndex={5} flip={true} />
      <ProductSection productIndex={6} flip={false} />
      <ProductSection productIndex={7} flip={true} />
    </div>
    <UseCases />
    <Stats />
    <FAQ />
    <FinalCTA />
  </main>
  <Footer />
</Layout>

<a id="skip-link" href="#main-content">Skip to main content</a>

<style is:global>
  #skip-link {
    position: absolute;
    top: -100%;
    left: 1rem;
    z-index: 9999;
    padding: 0.75rem 1.25rem;
    background: var(--color-brand-primary);
    color: white;
    font-weight: 600;
    border-radius: 0 0 0.5rem 0.5rem;
    text-decoration: none;
    transition: top 0.1s;
  }
  #skip-link:focus { top: 0; }

  #hero-wrap .url-input-wrap,
  .final-cta .url-input-wrap {
    background: rgba(255,255,255,0.06) !important;
    backdrop-filter: blur(24px) saturate(1.4);
    -webkit-backdrop-filter: blur(24px) saturate(1.4);
    border: 1px solid rgba(255,255,255,0.12) !important;
    box-shadow: 0 4px 30px rgba(0,0,0,0.2), inset 0 1px 0 rgba(255,255,255,0.05);
  }
  #hero-wrap .hero-panel {
    background: rgba(6,7,18,0.55) !important;
    backdrop-filter: blur(20px) saturate(1.3);
    -webkit-backdrop-filter: blur(20px) saturate(1.3);
    border: 1px solid rgba(255,255,255,0.12) !important;
  }
  #hero-wrap .panel-source-row { border-bottom: 1px solid rgba(255,255,255,0.1) !important; }
  #hero-wrap .core-inner {
    background: rgba(0,0,0,0.15) !important;
    border: 1px solid rgba(255,255,255,0.1) !important;
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
  }
  #hero-wrap .panel-core-section { border-bottom: 1px solid rgba(255,255,255,0.1) !important; }
  #hero-wrap .channel-node {
    background: rgba(255,255,255,0.03) !important;
    border: 1px solid rgba(255,255,255,0.1) !important;
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
  }
  #hero-wrap .panel-header-bar { border-bottom: 1px solid rgba(255,255,255,0.1) !important; }
  #hero-wrap .panel-ready-bar { border-top: 1px solid rgba(255,255,255,0.1) !important; }
  #hero-wrap .node-stat-text { color: #a78bfa !important; }
  #hero-wrap .hero-panel-mobile {
    background: rgba(10,10,20,0.3);
    backdrop-filter: blur(20px) saturate(1.3);
    -webkit-backdrop-filter: blur(20px) saturate(1.3);
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 1.25rem;
    padding: 1rem;
    box-shadow: 0 8px 32px rgba(0,0,0,0.3);
  }
  .final-cta {
    background: rgba(20,15,40,0.35) !important;
    backdrop-filter: blur(24px) saturate(1.4) !important;
    -webkit-backdrop-filter: blur(24px) saturate(1.4) !important;
    border-color: rgba(99,102,241,0.15) !important;
    box-shadow: 0 8px 40px rgba(0,0,0,0.3), inset 0 1px 0 rgba(255,255,255,0.04) !important;
    overflow: hidden;
    position: relative;
  }
  .final-cta > .absolute.inset-0.pointer-events-none { display: none !important; }
  #hero-wrap .grid-texture,
  #hero-wrap .absolute.inset-0.pointer-events-none[style*="radial-gradient"] { display: none !important; }
  #hero { background: transparent !important; }

  /* Hide hero headline + right panel */
  #hero-headline, #hero-sub, #hero-headline-m, #hero-sub-m { display: none !important; }
  #hero-panel, .hero-panel, .hero-panel-mobile { display: none !important; }
  #hero-eyebrow, #hero-eyebrow-m, #hero-cta-row, #hero-cta-m, .url-input-wrap, .hero-panel-mobile { display: none !important; }

</style>

<script>
  const ric = (window as any).requestIdleCallback || ((cb: Function) => setTimeout(cb, 1));
  ric(() => {

  // ── Types ──────────────────────────────────────────────────────────────────
  interface Account {
    platform: string;
    color: [number,number,number];
    username: string;
    followers: string;
  }
  interface PhoneNode {
    lat: number; lon: number;
    x3d: number; y3d: number; z3d: number;
    phoneNum: number;
    model: string;
    city: string;
    accounts: Account[];
    status: 'Active' | 'Posting' | 'Engaging';
    phase: number;
    postsToday: number;
  }
  interface BurstParticle {
    ringIdx: number;
    progress: number; // 0-1
    fromSx: number; fromSy: number;
    toSx: number; toSy: number;
    color: [number,number,number];
  }

  // ── Ring definitions ───────────────────────────────────────────────────────
  const RINGS = [
    { name: 'Instagram', color: [228, 64, 95]    as [number,number,number], radiusMult: 1.3, tilt: 0,                     speed: 0.30, count: 120, postCount: 2847 },
    { name: 'TikTok',    color: [0,  242, 234]   as [number,number,number], radiusMult: 1.5, tilt:  30*Math.PI/180,        speed: 0.20, count: 150, postCount: 3862 },
    { name: 'X',         color: [139, 92,  246]  as [number,number,number], radiusMult: 1.4, tilt: -20*Math.PI/180,        speed: 0.15, count: 130, postCount: 1832 },
  ];
  const TOTAL_RING_POSTS = RINGS.reduce((s,r)=>s+r.postCount,0);

  // ── Data ───────────────────────────────────────────────────────────────────
  const PLATFORMS = [
    { name: 'TikTok',    color: [0, 242, 234]   as [number,number,number] },
    { name: 'Instagram', color: [228, 64, 95]    as [number,number,number] },
    { name: 'X',         color: [139, 92, 246]   as [number,number,number] },
    { name: 'Reddit',    color: [255, 69, 0]     as [number,number,number] },
    { name: 'YouTube',   color: [255, 0, 0]      as [number,number,number] },
  ];
  const NICHES  = ['fitness','food','travel','tech','finance','fashion','gaming','beauty','sports','music'];
  const FNAMES  = ['alex','casey','jordan','taylor','morgan','riley','sage','quinn','blake','drew','luna','nova','rex','ivy','leo'];
  const MODELS  = ['iPhone 15','iPhone 14','Samsung S24','Pixel 8','iPhone 15 Pro','Samsung S23','OnePlus 12'];
  const STATUSES = ['Active','Posting','Engaging'] as const;
  const CITIES = [
    { name: 'London',      lat: 51.5,  lon: -0.1,   code: 'ldn', count: 15 },
    { name: 'New York',    lat: 40.7,  lon: -74.0,  code: 'nyc', count: 12 },
    { name: 'Los Angeles', lat: 34.0,  lon: -118.2, code: 'la',  count: 10 },
    { name: 'Tokyo',       lat: 35.7,  lon: 139.7,  code: 'tky', count: 10 },
    { name: 'Berlin',      lat: 52.5,  lon: 13.4,   code: 'bln', count: 8  },
    { name: 'Sydney',      lat: -33.9, lon: 151.2,  code: 'syd', count: 8  },
    { name: 'Dubai',       lat: 25.2,  lon: 55.3,   code: 'dxb', count: 7  },
    { name: 'Singapore',   lat: 1.3,   lon: 103.8,  code: 'sgp', count: 7  },
    { name: 'São Paulo',   lat: -23.5, lon: -46.6,  code: 'sao', count: 6  },
    { name: 'Mumbai',      lat: 19.1,  lon: 72.9,   code: 'mum', count: 6  },
    { name: 'Seoul',       lat: 37.6,  lon: 127.0,  code: 'sel', count: 5  },
    { name: 'Toronto',     lat: 43.7,  lon: -79.4,  code: 'tor', count: 5  },
  ];

  function r01(){return Math.random();}
  function rPick<T>(arr: readonly T[]): T {return arr[Math.floor(r01()*arr.length)];}
  function rRange(a: number,b: number){return a+r01()*(b-a);}

  // ── Globe math ─────────────────────────────────────────────────────────────
  function latLonTo3D(lat: number,lon: number):[number,number,number]{
    const phi=(90-lat)*Math.PI/180,theta=(lon+180)*Math.PI/180;
    return[Math.sin(phi)*Math.cos(theta),Math.cos(phi),Math.sin(phi)*Math.sin(theta)];
  }

  let W=0,H=0,dpr=1,GLOBE_R=1,GLOBE_CX=0,GLOBE_CY=0,rotY=0;

  function applyRotY(x: number,y: number,z: number):[number,number,number]{
    const c=Math.cos(rotY),s=Math.sin(rotY);return[x*c+z*s,y,-x*s+z*c];
  }
  function project(x: number,y: number,z: number){
    const[rx,ry,rz]=applyRotY(x,y,z);
    return{sx:GLOBE_CX+rx*GLOBE_R,sy:GLOBE_CY-ry*GLOBE_R,depth:rz,visible:rz>-0.1};
  }

  // Ring dot 3D position (before Y rotation) in globe radius units
  function ringDot3D(ringIdx: number, dotIdx: number, t: number): [number,number,number] {
    const ring=RINGS[ringIdx];
    const angle=(dotIdx/ring.count)*Math.PI*2+t*ring.speed;
    const R=ring.radiusMult;
    // Position in ring equatorial plane
    const x=R*Math.cos(angle);
    const y=0;
    const z=R*Math.sin(angle);
    // Tilt around X axis
    const ct=Math.cos(ring.tilt),st=Math.sin(ring.tilt);
    return[x, y*ct-z*st, y*st+z*ct];
  }

  // ── Generate phones ────────────────────────────────────────────────────────
  const phones: PhoneNode[]=[];
  let num=1;
  for(const city of CITIES){
    for(let i=0;i<city.count;i++){
      const spread=city.name==='Singapore'?0.5:1.5;
      const lat=city.lat+rRange(-spread,spread),lon=city.lon+rRange(-spread,spread);
      const[x3d,y3d,z3d]=latLonTo3D(lat,lon);
      const n=Math.floor(r01()*3)+2;
      const accs: Account[]=[];
      for(let a=0;a<n;a++){
        const pl=rPick(PLATFORMS);const fol=Math.floor(rRange(1000,80000));
        accs.push({platform:pl.name,color:pl.color,username:`@${rPick(FNAMES)}_${rPick(NICHES)}_${city.code}`,followers:fol>=1000?`${(fol/1000).toFixed(1)}k`:`${fol}`});
      }
      phones.push({lat,lon,x3d,y3d,z3d,phoneNum:num++,model:rPick(MODELS),city:city.name,accounts:accs,status:rPick(STATUSES),phase:r01()*Math.PI*2,postsToday:Math.floor(rRange(5,80))});
    }
  }
  const TOTAL_PHONES=phones.length,TOTAL_ACCOUNTS=phones.reduce((s,p)=>s+p.accounts.length,0);

  // ── Canvas ─────────────────────────────────────────────────────────────────
  const canvas=document.querySelector<HTMLCanvasElement>('.globe-canvas')!;
  const wrap=document.getElementById('hero-wrap')!;
  const ctx=canvas.getContext('2d')!;
  function resize(){
    dpr=window.devicePixelRatio||1;
    const rect=wrap.getBoundingClientRect();
    W=Math.round(rect.width);H=Math.round(rect.height);
    canvas.width=W*dpr;canvas.height=H*dpr;
    canvas.style.width=`${W}px`;canvas.style.height=`${H}px`;
    ctx.setTransform(dpr,0,0,dpr,0,0);
    GLOBE_R=Math.min(W,H)*0.27;GLOBE_CX=W*0.5;GLOBE_CY=H*0.48;
  }

  // ── Helpers ────────────────────────────────────────────────────────────────
  function rrect(c: CanvasRenderingContext2D,x: number,y: number,w: number,h: number,r: number){
    c.beginPath();c.moveTo(x+r,y);c.lineTo(x+w-r,y);c.quadraticCurveTo(x+w,y,x+w,y+r);
    c.lineTo(x+w,y+h-r);c.quadraticCurveTo(x+w,y+h,x+w-r,y+h);
    c.lineTo(x+r,y+h);c.quadraticCurveTo(x,y+h,x,y+h-r);
    c.lineTo(x,y+r);c.quadraticCurveTo(x,y,x+r,y);c.closePath();
  }

  function drawWireframe(){
    ctx.lineWidth=0.5;
    for(const lat of[-60,-30,0,30,60]){
      ctx.strokeStyle=lat===0?'rgba(192,38,211,0.06)':'rgba(192,38,211,0.05)';
      ctx.beginPath();let go=false;
      for(let lon=-180;lon<=180;lon+=3){const[x,y,z]=latLonTo3D(lat,lon);const p=project(x,y,z);if(p.visible){if(!go){ctx.moveTo(p.sx,p.sy);go=true;}else ctx.lineTo(p.sx,p.sy);}else{if(go){ctx.stroke();ctx.beginPath();go=false;}}}
      if(go)ctx.stroke();
    }
    for(let lon=0;lon<360;lon+=30){
      ctx.strokeStyle='rgba(192,38,211,0.05)';
      ctx.beginPath();let go=false;
      for(let lat=-90;lat<=90;lat+=3){const[x,y,z]=latLonTo3D(lat,lon);const p=project(x,y,z);if(p.visible){if(!go){ctx.moveTo(p.sx,p.sy);go=true;}else ctx.lineTo(p.sx,p.sy);}else{if(go){ctx.stroke();ctx.beginPath();go=false;}}}
      if(go)ctx.stroke();
    }
  }

  // ── Draw orbital rings ─────────────────────────────────────────────────────
  // Returns the screen position of a specific ring dot for burst targeting
  function drawRings(t: number, hoveredRingIdx: number): {sx:number;sy:number;ri:number}[] {
    const ringPositions: {sx:number;sy:number;ri:number}[] = [];
    for(let ri=0;ri<RINGS.length;ri++){
      const ring=RINGS[ri];
      const[cr,cg,cb]=ring.color;
      const bright=hoveredRingIdx===ri;
      const baseAlpha=bright?0.35:0.15;
      for(let di=0;di<ring.count;di++){
        const[x,y,z]=ringDot3D(ri,di,t);
        const p=project(x,y,z);
        // Ring dots: front=full alpha, back=very faint
        const alpha=p.depth>0?baseAlpha:0.03;
        ctx.globalAlpha=alpha;
        ctx.fillStyle=`rgb(${cr},${cg},${cb})`;
        ctx.beginPath();ctx.arc(p.sx,p.sy,1.5,0,Math.PI*2);ctx.fill();
        // Store one representative position per ring for burst targeting
        if(di===0 && p.depth>0) ringPositions.push({sx:p.sx,sy:p.sy,ri});
      }
      ctx.globalAlpha=1;
      // Ring label if hovered
      if(bright){
        // Find frontmost dot for label position
        let labelSx=GLOBE_CX,labelSy=GLOBE_CY-ring.radiusMult*GLOBE_R-10;
        for(let di=0;di<ring.count;di++){
          const[x,y,z]=ringDot3D(ri,di,t);
          const p=project(x,y,z);
          if(p.depth>0&&p.sy<labelSy){labelSx=p.sx;labelSy=p.sy;}
        }
        ctx.save();
        ctx.globalAlpha=0.7;ctx.fillStyle=`rgb(${cr},${cg},${cb})`;
        ctx.font='9px "SF Mono","Fira Code",monospace';ctx.textAlign='center';
        ctx.fillText(`${ring.name} — ${ring.postCount.toLocaleString()} posts in orbit`,labelSx,labelSy-6);
        ctx.restore();
      }
    }
    return ringPositions;
  }

  // ── Tooltip ────────────────────────────────────────────────────────────────
  function drawTooltip(phone: PhoneNode,sx: number,sy: number,hovRingIdx: number){
    const pad=14,lineH=17;
    const tooltipW=330,tooltipH=pad*2+lineH*(phone.accounts.length+3)+10;
    let tx=sx+18,ty=sy-tooltipH/2;
    if(tx+tooltipW>W-8)tx=sx-tooltipW-18;
    ty=Math.max(8,Math.min(H-tooltipH-8,ty));

    ctx.save();
    ctx.shadowColor='rgba(192,38,211,0.3)';ctx.shadowBlur=20;
    rrect(ctx,tx,ty,tooltipW,tooltipH,8);
    ctx.fillStyle='rgba(6,7,18,0.92)';ctx.fill();ctx.shadowBlur=0;
    rrect(ctx,tx,ty,tooltipW,tooltipH,8);
    ctx.fillStyle='rgba(192,38,211,0.07)';ctx.fill();
    ctx.strokeStyle='rgba(192,38,211,0.32)';ctx.lineWidth=1;ctx.stroke();
    ctx.fillStyle='rgba(192,38,211,0.7)';rrect(ctx,tx,ty,tooltipW,3,3);ctx.fill();

    let cy2=ty+pad+13;
    ctx.globalAlpha=1;ctx.textAlign='left';
    ctx.fillStyle='#c026d3';ctx.font='bold 11px "SF Mono","Fira Code",monospace';
    ctx.fillText(`PHONE #${phone.phoneNum}  \u00b7  ${phone.model}`,tx+pad,cy2);cy2+=lineH;
    ctx.fillStyle='rgba(255,255,255,0.45)';ctx.font='10px "SF Mono","Fira Code",monospace';
    ctx.fillText(`\u{1F4CD} ${phone.city}`,tx+pad,cy2);cy2+=lineH+3;

    const groups=new Map<string,{color:[number,number,number];entries:{u:string;f:string}[]}>();
    for(const acc of phone.accounts){if(!groups.has(acc.platform))groups.set(acc.platform,{color:acc.color,entries:[]});groups.get(acc.platform)!.entries.push({u:acc.username,f:acc.followers});}
    for(const[plat,{color,entries}]of groups){
      const[r,g,b]=color;
      ctx.fillStyle=`rgb(${r},${g},${b})`;ctx.beginPath();ctx.arc(tx+pad+5,cy2-4,4,0,Math.PI*2);ctx.fill();
      const cnt=entries.length>1?`${entries.length}\u00d7 `:'   ';
      const line=`${cnt}${plat.padEnd(10)} ${entries.map(e=>`${e.u} (${e.f})`).join('  ')}`;
      ctx.fillStyle='rgba(255,255,255,0.78)';ctx.font='10px "SF Mono","Fira Code",monospace';
      ctx.save();ctx.beginPath();ctx.rect(tx+pad+14,cy2-13,tooltipW-pad-22,16);ctx.clip();
      ctx.fillText(line,tx+pad+14,cy2);ctx.restore();cy2+=lineH;
    }
    cy2+=4;
    const sc=phone.status==='Active'?'rgba(74,222,128,0.9)':phone.status==='Posting'?'rgba(192,38,211,0.9)':'rgba(251,191,36,0.8)';
    ctx.fillStyle=sc;ctx.font='10px "SF Mono","Fira Code",monospace';
    ctx.fillText(`\u25cf ${phone.status}  \u00b7  ${phone.accounts.length} accounts  \u00b7  ${phone.postsToday} posts today`,tx+pad,cy2);
    ctx.restore();
  }

  // ── Hover ──────────────────────────────────────────────────────────────────
  let mouseX=-1,mouseY=-1,mouseOn=false;
  let hoveredPhone: PhoneNode|null=null;
  let hoveredRingIdx=-1;

  function findHovered(){
    if(!mouseOn)return null;
    let best: PhoneNode|null=null,bestD=20;
    for(const p of phones){const proj=project(p.x3d,p.y3d,p.z3d);if(!proj.visible)continue;const d=Math.sqrt((proj.sx-mouseX)**2+(proj.sy-mouseY)**2);if(d<bestD){bestD=d;best=p;}}
    return best;
  }
  function findHoveredRing(){
    if(!mouseOn)return -1;
    const dx=mouseX-GLOBE_CX,dy=mouseY-GLOBE_CY;
    const dist=Math.sqrt(dx*dx+dy*dy);
    for(let i=0;i<RINGS.length;i++){
      const ringR=RINGS[i].radiusMult*GLOBE_R;
      if(Math.abs(dist-ringR)<22)return i;
    }
    return -1;
  }

  // ── Burst particles ────────────────────────────────────────────────────────
  const bursts: BurstParticle[]=[];
  function spawnBurst(phone: PhoneNode, ringIdx: number, phoneSx: number, phoneSy: number, t: number){
    // Target: approximate ring position near phone's screen location
    const ring=RINGS[ringIdx];
    const dx=phoneSx-GLOBE_CX, dy=phoneSy-GLOBE_CY;
    const dist=Math.sqrt(dx*dx+dy*dy)||1;
    const targetR=ring.radiusMult*GLOBE_R;
    bursts.push({
      ringIdx,
      progress:0,
      fromSx:phoneSx, fromSy:phoneSy,
      toSx: GLOBE_CX+(dx/dist)*targetR,
      toSy: GLOBE_CY+(dy/dist)*targetR,
      color: ring.color,
    });
    if(bursts.length>30)bursts.splice(0,bursts.length-30);
  }

  // ── Coastline data ─────────────────────────────────────────────────────────
  const COASTLINES: [number,number][][] = [
    [[60,-140],[58,-138],[55,-133],[54,-130],[50,-128],[48,-124],[45,-124],[42,-124],[40,-124],[37,-122],[35,-120],[33,-117],[30,-115],[28,-113],[25,-110],[23,-107],[20,-105],[18,-103],[17,-100],[18,-96],[20,-97],[22,-97],[25,-97],[27,-97],[29,-95],[30,-90],[30,-88],[29,-85],[27,-82],[25,-80],[25,-78],[27,-80],[30,-81],[32,-80],[35,-76],[37,-76],[39,-74],[41,-72],[42,-71],[43,-70],[44,-68],[45,-67],[47,-65],[48,-64],[47,-61],[48,-59],[47,-56],[49,-55],[50,-57],[52,-56],[53,-57],[55,-60],[57,-62],[58,-64],[60,-65],[62,-66],[65,-63],[67,-62],[70,-60],[72,-58],[73,-55],[72,-50],[70,-48],[68,-45],[65,-42],[62,-42],[60,-45],[58,-50],[60,-55],[62,-60],[64,-65],[66,-68],[70,-75],[72,-80],[73,-85],[72,-90],[70,-95],[68,-100],[66,-108],[64,-115],[63,-120],[62,-130],[61,-135],[60,-140]],
    [[18,-103],[17,-100],[16,-97],[15,-93],[15,-90],[15,-87],[14,-84],[12,-83],[10,-84],[9,-83],[8,-80],[8,-77],[9,-76]],
    [[12,-72],[11,-75],[10,-76],[8,-77],[5,-77],[2,-79],[0,-80],[-2,-80],[-5,-81],[-7,-80],[-8,-79],[-10,-77],[-12,-77],[-15,-76],[-18,-71],[-20,-70],[-23,-70],[-25,-68],[-27,-66],[-30,-64],[-33,-62],[-35,-58],[-37,-57],[-40,-62],[-42,-64],[-45,-66],[-48,-68],[-50,-70],[-52,-70],[-54,-68],[-55,-66],[-54,-64],[-52,-68],[-48,-66],[-45,-64],[-42,-62],[-38,-57],[-35,-53],[-30,-50],[-25,-47],[-22,-41],[-18,-39],[-15,-39],[-12,-38],[-10,-36],[-5,-35],[-2,-42],[0,-50],[2,-53],[5,-58],[7,-60],[8,-62],[10,-67],[12,-72]],
    [[36,-6],[37,-2],[38,0],[40,0],[42,3],[43,5],[44,8],[45,12],[44,14],[42,16],[40,18],[38,20],[37,24],[38,26],[40,28],[42,30],[44,34],[42,42],[40,44],[38,40],[36,36],[35,32],[35,28],[36,24],[38,20],[40,16],[42,12],[44,8],[48,2],[50,2],[52,4],[54,8],[55,10],[56,12],[58,16],[60,18],[62,20],[64,24],[66,25],[68,28],[70,30],[71,28],[70,25],[68,16],[66,14],[64,10],[62,6],[60,5],[58,6],[56,8],[54,10],[52,7],[50,4],[48,0],[47,-2],[46,-1],[44,-1],[42,-4],[40,-4],[38,-5],[36,-6]],
    [[35,-6],[36,0],[37,10],[33,12],[30,10],[27,14],[24,17],[20,18],[18,16],[15,18],[12,16],[10,15],[8,10],[6,10],[5,5],[4,2],[4,-2],[2,-9],[0,-9],[-2,-10],[-5,-12],[-8,-13],[-10,-14],[-12,-15],[-15,-17],[-18,-12],[-22,-15],[-25,-15],[-28,-16],[-30,-18],[-33,-18],[-35,-20],[-34,-24],[-33,-27],[-30,-30],[-27,-33],[-25,-34],[-20,-35],[-18,-37],[-15,-40],[-10,-40],[-5,-42],[-2,-44],[2,-45],[5,-44],[8,-42],[10,-42],[12,-44],[15,-42],[18,-40],[20,-37],[22,-36],[25,-35],[28,-33],[30,-32],[32,-28],[33,-22],[34,-18],[35,-12],[35,-6]],
    [[42,30],[44,34],[46,38],[48,40],[50,42],[52,44],[55,50],[58,55],[60,60],[62,65],[63,70],[65,75],[68,80],[70,85],[72,100],[70,130],[68,140],[65,142],[62,140],[60,135],[55,135],[50,132],[48,130],[45,128],[42,125],[40,120],[38,118],[36,115],[34,110],[30,105],[28,100],[25,98],[22,95],[20,92],[18,88],[15,82],[12,80],[10,78],[8,76],[5,73],[2,70],[0,65],[-2,60],[-5,55],[-8,50],[-6,42],[0,38],[5,35],[10,32],[15,30],[20,28],[25,30],[28,32],[30,34],[33,36],[35,35],[38,35],[40,32],[42,30]],
    [[28,68],[25,70],[22,72],[20,73],[18,73],[15,75],[12,77],[10,78],[8,77],[10,76],[12,74],[15,73],[18,72],[20,70],[22,68],[25,67],[28,68]],
    [[5,98],[2,104],[0,105],[-2,107],[-5,106],[-7,108],[-8,112],[-7,115],[-5,118],[-3,120],[-1,118],[1,110],[3,106],[5,98]],
    [[33,130],[34,132],[35,135],[36,138],[38,140],[40,140],[42,142],[44,144],[45,142],[43,141],[40,139],[38,137],[36,136],[34,133],[33,130]],
    [[-12,130],[-14,127],[-16,124],[-18,122],[-20,118],[-22,115],[-25,114],[-28,114],[-30,115],[-32,116],[-34,118],[-35,120],[-36,137],[-37,140],[-38,145],[-37,148],[-35,150],[-33,152],[-30,153],[-28,154],[-25,153],[-22,150],[-20,149],[-18,147],[-16,146],[-14,142],[-13,136],[-12,130]],
    [[50,-5],[51,-3],[52,-1],[53,0],[54,0],[55,-2],[56,-3],[57,-5],[58,-5],[58,-3],[57,-2],[56,0],[55,1],[54,1],[52,2],[51,1],[50,0],[50,-5]],
    [[-35,174],[-37,175],[-38,177],[-40,176],[-42,172],[-44,170],[-46,168],[-45,167],[-43,170],[-41,172],[-39,175],[-37,176],[-35,174]],
  ];

  // ── Frame ──────────────────────────────────────────────────────────────────
  function frame(){
    requestAnimationFrame(frame);
    const t=performance.now()*0.001;
    ctx.clearRect(0,0,W,H);
    rotY+=0.003;

    // Globe glow
    const grd=ctx.createRadialGradient(GLOBE_CX,GLOBE_CY,0,GLOBE_CX,GLOBE_CY,GLOBE_R*1.8);
    grd.addColorStop(0,'rgba(192,38,211,0.04)');grd.addColorStop(1,'rgba(192,38,211,0)');
    ctx.fillStyle=grd;ctx.beginPath();ctx.arc(GLOBE_CX,GLOBE_CY,GLOBE_R*1.8,0,Math.PI*2);ctx.fill();

    // Draw rings behind globe
    hoveredRingIdx=findHoveredRing();
    drawRings(t, hoveredPhone?-1:hoveredRingIdx); // suppress ring highlight if phone is hovered

    drawWireframe();

    // ── Continent coastlines ──────────────────────────────────────────────
    ctx.lineWidth = 1;
    ctx.strokeStyle = 'rgba(192,38,211,0.15)';
    for (const coast of COASTLINES) {
      ctx.beginPath(); let cpv = false;
      for (const [clat, clon] of coast) {
        const [cx3,cy3,cz3] = latLonTo3D(clat, clon);
        const cp = project(cx3, cy3, cz3);
        if (cp.visible) { if (!cpv) ctx.moveTo(cp.sx, cp.sy); else ctx.lineTo(cp.sx, cp.sy); cpv = true; }
        else { if (cpv) { ctx.stroke(); ctx.beginPath(); } cpv = false; }
      }
      if (cpv) ctx.stroke();
    }

    hoveredPhone=findHovered();

    // Highlight rings corresponding to hovered phone's platforms
    if(hoveredPhone){
      const phonePlats=new Set(hoveredPhone.accounts.map(a=>a.platform));
      for(let ri=0;ri<RINGS.length;ri++){
        if(phonePlats.has(RINGS[ri].name)){
          const[cr,cg,cb]=RINGS[ri].color;
          for(let di=0;di<RINGS[ri].count;di++){
            const[x,y,z]=ringDot3D(ri,di,t);
            const p=project(x,y,z);
            if(p.depth>0){
              ctx.globalAlpha=0.45;ctx.fillStyle=`rgb(${cr},${cg},${cb})`;
              ctx.beginPath();ctx.arc(p.sx,p.sy,1.8,0,Math.PI*2);ctx.fill();
            }
          }
          ctx.globalAlpha=1;
        }
      }
    }

    // Phone nodes + burst spawning
    for(const phone of phones){
      const p=project(phone.x3d,phone.y3d,phone.z3d);
      if(!p.visible){
        ctx.globalAlpha=0.07;ctx.fillStyle='#c026d3';
        ctx.beginPath();ctx.arc(p.sx,p.sy,1.5,0,Math.PI*2);ctx.fill();
        ctx.globalAlpha=1;continue;
      }
      const hov=hoveredPhone===phone;
      let alpha=0.35,rad=3;
      if(phone.status==='Posting'){alpha=0.35+Math.sin(t*3+phone.phase)*0.15;if(Math.sin(t*2+phone.phase)>0.85)alpha=0.85;}
      else if(phone.status==='Active'){alpha=0.35+Math.sin(t*0.8+phone.phase)*0.08;}
      if(hov){alpha=1;rad=6;}
      ctx.globalAlpha=alpha;ctx.fillStyle='#c026d3';
      ctx.beginPath();ctx.arc(p.sx,p.sy,rad,0,Math.PI*2);ctx.fill();
      const[pr,pg,pb]=phone.accounts[0].color;
      ctx.globalAlpha=hov?1:alpha*0.9;ctx.fillStyle=`rgb(${pr},${pg},${pb})`;
      ctx.beginPath();ctx.arc(p.sx,p.sy,hov?3:1.5,0,Math.PI*2);ctx.fill();
      ctx.globalAlpha=1;

      // Spawn burst periodically per phone per ring
      for(let ri=0;ri<RINGS.length;ri++){
        const hasPlat=phone.accounts.some(a=>a.platform===RINGS[ri].name);
        if(!hasPlat)continue;
        const sig=Math.sin(t*0.5+phone.phase+ri*2.1);
        if(sig>0.97 && p.visible){spawnBurst(phone,ri,p.sx,p.sy,t);}
      }
    }

    // Burst particles
    for(let i=bursts.length-1;i>=0;i--){
      const b=bursts[i];
      b.progress+=0.025;
      if(b.progress>=1){bursts.splice(i,1);continue;}
      const bx=b.fromSx+(b.toSx-b.fromSx)*b.progress;
      const by=b.fromSy+(b.toSy-b.fromSy)*b.progress;
      const[cr,cg,cb]=b.color;
      ctx.globalAlpha=0.7*(1-b.progress);
      ctx.fillStyle=`rgb(${cr},${cg},${cb})`;
      ctx.beginPath();ctx.arc(bx,by,2,0,Math.PI*2);ctx.fill();
      ctx.globalAlpha=1;
    }

    if(hoveredPhone){const p=project(hoveredPhone.x3d,hoveredPhone.y3d,hoveredPhone.z3d);drawTooltip(hoveredPhone,p.sx,p.sy,hoveredRingIdx);}

    ctx.save();ctx.textAlign='right';ctx.font='11px "SF Mono","Fira Code",monospace';
    ctx.fillStyle='rgba(192,38,211,0.6)';
    ctx.fillText(`${TOTAL_PHONES} phones  \u00b7  3 orbital platforms  \u00b7  ${TOTAL_RING_POSTS.toLocaleString()} posts in orbit`,W-16,24);
    ctx.restore();
  }

  // ── Mouse ──────────────────────────────────────────────────────────────────
  wrap.addEventListener('mousemove',(e)=>{const r=wrap.getBoundingClientRect();mouseX=e.clientX-r.left;mouseY=e.clientY-r.top;mouseOn=true;});
  wrap.addEventListener('mouseleave',()=>{mouseOn=false;hoveredPhone=null;hoveredRingIdx=-1;});
  wrap.addEventListener('touchstart',()=>{mouseOn=false;hoveredPhone=null;hoveredRingIdx=-1;},{passive:true});

  // ── Boot ───────────────────────────────────────────────────────────────────
  const ro=new ResizeObserver(()=>resize());ro.observe(wrap);resize();
  requestAnimationFrame(frame);
  requestAnimationFrame(()=>{canvas.style.opacity='1';});
  const gt=wrap.querySelector('.grid-texture') as HTMLElement|null;if(gt)gt.style.display='none';

  // ── FinalCTA canvas ────────────────────────────────────────────────────────
  {
    const cta=document.querySelector('.final-cta') as HTMLElement|null;
    if(cta){
      const cc=document.createElement('canvas');
      cc.setAttribute('aria-hidden','true');
      cc.style.cssText='position:absolute;inset:0;z-index:0;pointer-events:none;border-radius:inherit;opacity:0;transition:opacity 2s ease-in;';
      cta.style.position='relative';cta.insertBefore(cc,cta.firstChild);
      const cx2=cc.getContext('2d')!;
      interface CDot{x:number;y:number;phase:number;}
      let cdots:CDot[]=[],cdpr=1;
      function buildCta(){cdots=[];const cw=cc.width/cdpr,ch=cc.height/cdpr;for(let gy=0;gy<ch;gy+=18)for(let gx=0;gx<cw;gx+=18)cdots.push({x:gx,y:gy,phase:r01()*Math.PI*2});}
      function resizeCta(){cdpr=window.devicePixelRatio||1;const rect=cta!.getBoundingClientRect();const cw=Math.round(rect.width),ch=Math.round(rect.height);cc.width=cw*cdpr;cc.height=ch*cdpr;cc.style.width=`${cw}px`;cc.style.height=`${ch}px`;cx2.setTransform(cdpr,0,0,cdpr,0,0);buildCta();}
      function frameCta(){requestAnimationFrame(frameCta);const cw=cc.width/cdpr,ch=cc.height/cdpr,t=performance.now()*0.001;cx2.clearRect(0,0,cw,ch);for(const d of cdots){cx2.globalAlpha=0.12+Math.sin(t*0.6+d.phase)*0.05;cx2.fillStyle='#c026d3';cx2.beginPath();cx2.arc(d.x,d.y,1.5,0,Math.PI*2);cx2.fill();}cx2.globalAlpha=1;}
      const ro2=new ResizeObserver(()=>resizeCta());ro2.observe(cta);resizeCta();
      requestAnimationFrame(frameCta);requestAnimationFrame(()=>{cc.style.opacity='1';});
      const ctgt=cta.querySelector('.grid-texture') as HTMLElement|null;if(ctgt)ctgt.style.display='none';
    }
  }

  }); // end ric
</script>
