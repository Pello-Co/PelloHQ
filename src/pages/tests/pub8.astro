---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/layout/Header.astro';
import Footer from '../../components/layout/Footer.astro';
import HeroPello from '../../components/sections/HeroPello.astro';
import HowItWorks from '../../components/sections/HowItWorks.astro';
import ProductSection from '../../components/sections/ProductSection.astro';
import UseCases from '../../components/sections/UseCases.astro';
import Stats from '../../components/sections/Stats.astro';
import FAQ from '../../components/sections/FAQ.astro';
import FinalCTA from '../../components/sections/FinalCTA.astro';
---

<Layout title="PelloPublish — Content Rain Matrix" description="PelloPublish: AI-generated content across all platforms at scale. Watch your content matrix grow.">
  <Header />
  <main id="main-content">
    <div id="hero-wrap" class="relative">
      <canvas class="pub8-dot-canvas" aria-hidden="true" style="position:absolute;inset:0;z-index:1;pointer-events:none;will-change:transform;opacity:0;transition:opacity 1.5s ease-in;"></canvas>
      <HeroPello />
    </div>

    <HowItWorks />

    <div id="products">
      <ProductSection productIndex={0} flip={false} />
      <ProductSection productIndex={1} flip={true} />
      <ProductSection productIndex={2} flip={false} />
      <ProductSection productIndex={3} flip={true} />
      <ProductSection productIndex={4} flip={false} />
      <ProductSection productIndex={5} flip={true} />
      <ProductSection productIndex={6} flip={false} />
      <ProductSection productIndex={7} flip={true} />
    </div>

    <UseCases />
    <Stats />
    <FAQ />
    <FinalCTA />
  </main>
  <Footer />
</Layout>

<a id="skip-link" href="#main-content">Skip to main content</a>

<style is:global>
  #skip-link {
    position: absolute;
    top: -100%;
    left: 1rem;
    z-index: 9999;
    padding: 0.75rem 1.25rem;
    background: var(--color-brand-primary);
    color: white;
    font-weight: 600;
    border-radius: 0 0 0.5rem 0.5rem;
    text-decoration: none;
    transition: top 0.1s;
  }
  #skip-link:focus { top: 0; }

  #hero-wrap .url-input-wrap,
  .final-cta .url-input-wrap {
    background: rgba(255,255,255,0.06) !important;
    backdrop-filter: blur(24px) saturate(1.4);
    -webkit-backdrop-filter: blur(24px) saturate(1.4);
    border: 1px solid rgba(255,255,255,0.12) !important;
    box-shadow: 0 4px 30px rgba(0,0,0,0.2), inset 0 1px 0 rgba(255,255,255,0.05);
  }
  #hero-wrap .hero-panel {
    background: rgba(6,7,18,0.55) !important;
    backdrop-filter: blur(20px) saturate(1.3);
    -webkit-backdrop-filter: blur(20px) saturate(1.3);
    border: 1px solid rgba(255,255,255,0.12) !important;
  }
  #hero-wrap .panel-source-row { border-bottom: 1px solid rgba(255,255,255,0.1) !important; }
  #hero-wrap .core-inner {
    background: rgba(0,0,0,0.15) !important;
    border: 1px solid rgba(255,255,255,0.1) !important;
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
  }
  #hero-wrap .panel-core-section { border-bottom: 1px solid rgba(255,255,255,0.1) !important; }
  #hero-wrap .channel-node {
    background: rgba(255,255,255,0.03) !important;
    border: 1px solid rgba(255,255,255,0.1) !important;
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
  }
  #hero-wrap .panel-header-bar { border-bottom: 1px solid rgba(255,255,255,0.1) !important; }
  #hero-wrap .panel-ready-bar { border-top: 1px solid rgba(255,255,255,0.1) !important; }
  #hero-wrap .node-stat-text { color: #a78bfa !important; }
  #hero-wrap .hero-panel-mobile {
    background: rgba(10,10,20,0.3);
    backdrop-filter: blur(20px) saturate(1.3);
    -webkit-backdrop-filter: blur(20px) saturate(1.3);
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 1.25rem;
    padding: 1rem;
    box-shadow: 0 8px 32px rgba(0,0,0,0.3);
  }
  .final-cta {
    background: rgba(20,15,40,0.35) !important;
    backdrop-filter: blur(24px) saturate(1.4) !important;
    -webkit-backdrop-filter: blur(24px) saturate(1.4) !important;
    border-color: rgba(168,85,247,0.15) !important;
    box-shadow: 0 8px 40px rgba(0,0,0,0.3), inset 0 1px 0 rgba(255,255,255,0.04) !important;
    overflow: hidden;
    position: relative;
  }
  .final-cta > .absolute.inset-0.pointer-events-none { display: none !important; }
  #hero-wrap .grid-texture,
  #hero-wrap .absolute.inset-0.pointer-events-none[style*="radial-gradient"] { display: none !important; }
  #hero { background: transparent !important; }
</style>

<script>
  const ric = (window as any).requestIdleCallback || ((cb: Function) => setTimeout(cb, 1));

  ric(() => {

  // ─── Platform definitions ─────────────────────────────────────────────────
  interface Platform { name: string; color: [number,number,number]; weight: number; }
  const PLATFORM_TMPLS: Platform[] = [
    { name: "Instagram",  color: [225, 48,  108], weight: 0.25 },
    { name: "LinkedIn",   color: [10,  102, 194], weight: 0.20 },
    { name: "Twitter/X",  color: [139, 92,  246], weight: 0.20 },
    { name: "TikTok",     color: [0,   242, 234], weight: 0.12 },
    { name: "Blog",       color: [168, 85,  247], weight: 0.13 },
    { name: "YouTube",    color: [255, 0,   0  ], weight: 0.10 },
  ];

  const PLATFORM_CUM: number[] = [];
  { let s = 0; for (const p of PLATFORM_TMPLS) { s += p.weight; PLATFORM_CUM.push(s); } }

  const CONTENT_TYPES = ["Carousel","Reel","Story","Blog Post","Thread","Short","Article","UGC Clip","Infographic","Poll"];
  const TOPICS = [
    "5 Ways AI is Changing {industry}",
    "Behind the Scenes: How We {action}",
    "Why {topic} Matters More Than Ever",
    "{number} Tips for Better {goal}",
    "The Future of {industry} in 2026",
    "How {company} Grew 3x with {strategy}",
    "Stop Doing {mistake} — Do This Instead",
    "We Tested {strategy} for 30 Days",
  ];
  const INDUSTRIES = ["SaaS","E-commerce","Healthcare","Finance","Education","Real Estate"];
  const GOALS      = ["Engagement","Conversions","Brand Awareness","Lead Quality"];
  const STRATEGIES = ["Content Repurposing","AI Writing","Video-First","Community Building"];
  const POST_SNIPPETS = [
    "We analysed 10,000 social posts and found that...",
    "The #1 mistake most brands make with content is...",
    "Here's the framework we use to generate 60+ posts/month:",
    "AI-generated content gets 2.3x more engagement when...",
    "Your audience doesn't care about your product. They care about...",
    "Content repurposing is the single highest-ROI strategy because...",
    "We tested posting 3x/day vs 1x/day for 30 days. Results:",
    "The future of brand content is AI-assisted, human-directed.",
  ];
  const ENGAGEMENTS = ["1.2k likes","847 likes","2.3k views","412 shares","156 comments","3.1k reach","967 saves"];

  function pick<T>(arr: readonly T[]): T { return arr[Math.floor(Math.random() * arr.length)]; }

  function pickPlatform(): number {
    const r = Math.random();
    for (let i = 0; i < PLATFORM_CUM.length; i++) if (r < PLATFORM_CUM[i]) return i;
    return PLATFORM_TMPLS.length - 1;
  }

  function fillTopic(tpl: string): string {
    return tpl
      .replace('{industry}', pick(INDUSTRIES))
      .replace('{action}',   pick(['Scale','Automate','Grow','Optimise']))
      .replace('{topic}',    pick(['Content Marketing','AI Writing','Video Strategy']))
      .replace('{number}',   pick(['5','7','10','12']))
      .replace('{goal}',     pick(GOALS))
      .replace('{company}',  pick(['TechCo','ScaleUp','GrowthHQ']))
      .replace('{strategy}', pick(STRATEGIES))
      .replace('{mistake}',  pick(['Batch Posting','Keyword Stuffing','Ignoring Comments']));
  }

  // ─── Constants ────────────────────────────────────────────────────────────
  const GRID             = 10;
  const INFLUENCE_RADIUS = 200;
  const PUSH_STRENGTH    = 8;
  const LERP_SPEED       = 0.035;
  const HOVER_RADIUS     = 15;
  const PROXIMITY_RADIUS = 60;
  const STATUS_PUBLISHED = 0;
  const STATUS_SCHEDULED = 1;
  const STATUS_DRAFT     = 2;

  // ─── Dot type ─────────────────────────────────────────────────────────────
  interface Dot {
    gx: number; gy: number;
    x: number;  y: number;
    platformIdx: number;
    contentType: string;
    topicTitle: string;
    snippet: string;
    engagement: string;
    status: number;
    birthTime: number;
    phase: number;
  }

  // ─── Canvas setup ─────────────────────────────────────────────────────────
  const canvas = document.querySelector<HTMLCanvasElement>('.pub8-dot-canvas')!;
  const wrap   = document.getElementById('hero-wrap')!;
  const ctx    = canvas.getContext('2d')!;

  let dots: Dot[] = [];
  let mouse: { x: number; y: number } | null = null;
  let lastMX = -1, lastMY = -1;
  let hoveredIdx = -1;
  let dpr = 1;
  let startTime = 0;

  function buildGrid() {
    dots = [];
    const W    = canvas.width / dpr;
    const H    = canvas.height / dpr;
    const cols = Math.ceil(W / GRID) + 1;
    const rows = Math.ceil(H / GRID) + 1;
    const cx   = W / 2, cy = H / 2;

    for (let r = 0; r < rows; r++) {
      for (let c = 0; c < cols; c++) {
        const gx   = c * GRID;
        const gy   = r * GRID;
        const pIdx = pickPlatform();
        const sr   = Math.random();
        const status = sr < 0.70 ? STATUS_PUBLISHED : sr < 0.90 ? STATUS_SCHEDULED : STATUS_DRAFT;
        const dist = Math.sqrt((gx - cx) ** 2 + (gy - cy) ** 2);
        dots.push({
          gx, gy, x: gx, y: gy,
          platformIdx: pIdx,
          contentType: pick(CONTENT_TYPES),
          topicTitle: fillTopic(pick(TOPICS)),
          snippet: pick(POST_SNIPPETS),
          engagement: pick(ENGAGEMENTS),
          status,
          birthTime: dist * 0.003,
          phase: Math.random() * Math.PI * 2,
        });
      }
    }
    startTime = performance.now() * 0.001;
  }

  function resize() {
    dpr = window.devicePixelRatio || 1;
    const rect = wrap.getBoundingClientRect();
    const w = Math.round(rect.width);
    const h = Math.round(rect.height);
    canvas.width  = w * dpr;
    canvas.height = h * dpr;
    canvas.style.width  = `${w}px`;
    canvas.style.height = `${h}px`;
    ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
    buildGrid();
  }

  function findNearest(mx: number, my: number, elapsed: number): number {
    let best = -1, bestD = HOVER_RADIUS;
    for (let i = 0; i < dots.length; i++) {
      const d = dots[i];
      if (elapsed - d.birthTime < 0) continue;
      const dx = d.x - mx, dy = d.y - my;
      const dist = Math.sqrt(dx * dx + dy * dy);
      if (dist < bestD) { bestD = dist; best = i; }
    }
    return best;
  }

  function smoothstep(x: number): number {
    x = Math.max(0, Math.min(1, x));
    return x * x * (3 - 2 * x);
  }

  function rrect(c: CanvasRenderingContext2D, x: number, y: number, w: number, h: number, r: number) {
    c.beginPath();
    c.moveTo(x + r, y);
    c.lineTo(x + w - r, y);
    c.quadraticCurveTo(x + w, y, x + w, y + r);
    c.lineTo(x + w, y + h - r);
    c.quadraticCurveTo(x + w, y + h, x + w - r, y + h);
    c.lineTo(x + r, y + h);
    c.quadraticCurveTo(x, y + h, x, y + h - r);
    c.lineTo(x, y + r);
    c.quadraticCurveTo(x, y, x + r, y);
    c.closePath();
  }

  // ─── Tooltip ──────────────────────────────────────────────────────────────
  function drawTooltip(dot: Dot) {
    const W = canvas.width / dpr;
    const px = dot.x, py = dot.y;
    const [r, g, b] = PLATFORM_TMPLS[dot.platformIdx].color;
    const platformName = PLATFORM_TMPLS[dot.platformIdx].name;

    const pad      = 16;
    const tooltipW = 360;
    const tooltipH = 150;

    let tx = px - tooltipW / 2;
    let ty = py - tooltipH - 20;
    tx = Math.max(8, Math.min(W - tooltipW - 8, tx));
    if (ty < 8) ty = py + 20;

    ctx.save();

    // Connecting line
    ctx.globalAlpha = 0.18;
    ctx.strokeStyle = `rgb(${r},${g},${b})`;
    ctx.lineWidth = 1;
    ctx.beginPath();
    ctx.moveTo(px, py - 6);
    ctx.lineTo(px, ty < py ? ty + tooltipH : ty);
    ctx.stroke();

    ctx.globalAlpha = 1;

    // Shadow/glow
    ctx.shadowColor = `rgba(${r},${g},${b},0.2)`;
    ctx.shadowBlur = 30;
    rrect(ctx, tx, ty, tooltipW, tooltipH, 10);
    ctx.fillStyle = 'rgba(6,7,18,0.72)';
    ctx.fill();
    ctx.shadowBlur = 0;
    ctx.shadowColor = 'transparent';

    // Tint
    rrect(ctx, tx, ty, tooltipW, tooltipH, 10);
    ctx.fillStyle = `rgba(${r},${g},${b},0.08)`;
    ctx.fill();

    // Border
    ctx.strokeStyle = `rgba(${r},${g},${b},0.3)`;
    ctx.lineWidth = 1;
    ctx.stroke();

    // Accent top bar
    ctx.globalAlpha = 0.6;
    ctx.fillStyle = `rgb(${r},${g},${b})`;
    rrect(ctx, tx, ty, tooltipW, 3, 3);
    ctx.fill();

    // Row 1: Platform icon dot + name
    ctx.globalAlpha = 1;
    ctx.fillStyle = `rgb(${r},${g},${b})`;
    ctx.beginPath();
    ctx.arc(tx + pad + 5, ty + pad + 8, 4, 0, Math.PI * 2);
    ctx.fill();
    ctx.font = 'bold 11px "SF Mono","Fira Code",monospace';
    ctx.textAlign = 'left';
    ctx.fillText(platformName.toUpperCase(), tx + pad + 14, ty + pad + 12);

    // Row 2: Format
    ctx.globalAlpha = 0.5;
    ctx.fillStyle = '#ffffff';
    ctx.font = '10px "SF Mono","Fira Code",monospace';
    ctx.fillText(`FORMAT: ${dot.contentType}`, tx + pad, ty + pad + 30);

    // Row 3: Topic title
    ctx.globalAlpha = 1;
    ctx.fillStyle = '#ffffff';
    ctx.font = 'bold 13px -apple-system,system-ui,BlinkMacSystemFont,sans-serif';
    ctx.save();
    ctx.beginPath();
    ctx.rect(tx + pad, ty + pad + 34, tooltipW - pad * 2, 20);
    ctx.clip();
    ctx.fillText(dot.topicTitle, tx + pad, ty + pad + 50);
    ctx.restore();

    // Row 4: Snippet
    ctx.globalAlpha = 0.72;
    ctx.fillStyle = '#ffffff';
    ctx.font = 'italic 10px -apple-system,system-ui,BlinkMacSystemFont,sans-serif';
    const snippetText = dot.snippet.length > 60 ? dot.snippet.slice(0, 60) + '\u2026' : dot.snippet;
    ctx.fillText(snippetText, tx + pad, ty + pad + 70);

    // Row 5: Status
    const isPublished = dot.status === STATUS_PUBLISHED;
    const isScheduled = dot.status === STATUS_SCHEDULED;
    const statusStr   = isPublished ? '\u25CF Published' : isScheduled ? '\u25D0 Scheduled' : '\u25CB Draft';
    const statusColor = isPublished ? 'rgba(74,222,128,0.9)' : isScheduled ? 'rgba(251,191,36,0.8)' : 'rgba(156,163,175,0.7)';
    ctx.globalAlpha = 0.9;
    ctx.fillStyle = statusColor;
    ctx.font = '10px "SF Mono","Fira Code",monospace';
    ctx.fillText(statusStr, tx + pad, ty + pad + 90);

    // Row 6: Engagement (published only)
    if (isPublished) {
      ctx.globalAlpha = 0.8;
      ctx.fillStyle = 'rgba(74,222,128,0.9)';
      ctx.font = '10px "SF Mono","Fira Code",monospace';
      ctx.fillText(dot.engagement, tx + pad, ty + pad + 108);
    }

    ctx.restore();
  }

  // ─── Frame loop ───────────────────────────────────────────────────────────
  function frame() {
    requestAnimationFrame(frame);

    const W       = canvas.width / dpr;
    const H       = canvas.height / dpr;
    const t       = performance.now() * 0.001;
    const elapsed = t - startTime;

    ctx.clearRect(0, 0, W, H);

    if (mouse && (mouse.x !== lastMX || mouse.y !== lastMY)) {
      hoveredIdx = findNearest(mouse.x, mouse.y, elapsed);
      lastMX = mouse.x; lastMY = mouse.y;
    } else if (!mouse) {
      hoveredIdx = -1;
    }

    const mx = mouse ? mouse.x : -99999;
    const my = mouse ? mouse.y : -99999;
    let visibleCount = 0;

    for (let i = 0; i < dots.length; i++) {
      const dot = dots[i];

      const age = elapsed - dot.birthTime;
      if (age < 0) continue;
      const fadeIn = Math.min(1, age / 0.3);
      if (fadeIn < 0.005) continue;

      // Pulse ripple
      const PULSE_INTERVAL = 8;
      const PULSE_SPEED    = 0.003;
      const PULSE_DURATION = 0.6;
      const cx = W / 2, cy = H / 2;
      const distFromCenter = Math.sqrt((dot.gx - cx) ** 2 + (dot.gy - cy) ** 2);
      const dotDelay    = distFromCenter * PULSE_SPEED;
      const pulseElapsed = elapsed - 5;
      let pulseAlpha = 0;
      if (pulseElapsed > 0) {
        const cycleTime   = pulseElapsed % PULSE_INTERVAL;
        const dotCycleAge = cycleTime - dotDelay;
        if (dotCycleAge > 0 && dotCycleAge < PULSE_DURATION) {
          const pt = dotCycleAge / PULSE_DURATION;
          pulseAlpha = Math.sin(pt * Math.PI) * 0.3;
        }
      }

      visibleCount++;

      // Mouse push
      let targetX = dot.gx, targetY = dot.gy;
      const pdx = dot.gx - mx, pdy = dot.gy - my;
      const pdist = Math.sqrt(pdx * pdx + pdy * pdy);
      if (pdist < INFLUENCE_RADIUS) {
        const raw = 1 - pdist / INFLUENCE_RADIUS;
        const s   = raw * raw * (3 - 2 * raw);
        const inf = s * s * (3 - 2 * s);
        const pushMag = Math.pow(inf, 3) * PUSH_STRENGTH;
        const len = pdist || 1;
        targetX += (pdx / len) * pushMag;
        targetY += (pdy / len) * pushMag;
      }
      dot.x += (targetX - dot.x) * LERP_SPEED;
      dot.y += (targetY - dot.y) * LERP_SPEED;

      // Vertical fade-out
      const fadeZone = H * 0.78;
      let vFade = 1;
      if (dot.gy > fadeZone) vFade = 1 - Math.min(1, (dot.gy - fadeZone) / (H - fadeZone));
      if (vFade < 0.005) continue;

      const [r, g, b] = PLATFORM_TMPLS[dot.platformIdx].color;

      let alpha: number;
      let radius: number;

      if (dot.status === STATUS_PUBLISHED) {
        alpha  = 0.30 + Math.sin(t * 1.2 + dot.phase) * 0.10;
        radius = 2;
      } else if (dot.status === STATUS_SCHEDULED) {
        alpha  = 0.20;
        radius = 2;
      } else {
        // Draft — dim + size pulse
        alpha  = 0.10;
        radius = 1.8 + Math.sin(t * 0.8 + dot.phase) * 0.2;
      }

      if (hoveredIdx === i) {
        alpha = 0.9; radius = 6;
      }

      if (hoveredIdx !== i) {
        const ldx = dot.x - mx, ldy = dot.y - my;
        const ldist = Math.sqrt(ldx * ldx + ldy * ldy);
        if (ldist < PROXIMITY_RADIUS) {
          const pf = smoothstep(1 - ldist / PROXIMITY_RADIUS);
          radius += pf * 1.0;
          if (hoveredIdx < 0) alpha += pf * 0.10;
        }
      }

      alpha = Math.min(0.95, alpha + pulseAlpha);

      ctx.globalAlpha = alpha * fadeIn * vFade;
      ctx.fillStyle   = `rgb(${r},${g},${b})`;
      ctx.beginPath();
      ctx.arc(dot.x, dot.y, Math.max(0.5, radius), 0, Math.PI * 2);
      ctx.fill();
    }

    ctx.globalAlpha = 1;

    if (hoveredIdx >= 0) {
      const dot = dots[hoveredIdx];
      if (elapsed - dot.birthTime >= 0) drawTooltip(dot);
    }

    // Counter
    const counterX = W - 16;
    ctx.save();
    ctx.textAlign  = 'right';
    ctx.globalAlpha = 1;
    ctx.fillStyle  = 'rgba(168,85,247,0.55)';
    ctx.font       = '11px "SF Mono","Fira Code",monospace';
    ctx.fillText(`${visibleCount.toLocaleString()} pieces generated`, counterX, 24);
    ctx.globalAlpha = 0.28;
    ctx.fillStyle   = '#ffffff';
    ctx.font        = '9px "SF Mono","Fira Code",monospace';
    ctx.fillText('6 platforms \u00b7 10 formats', counterX, 38);
    ctx.restore();
  }

  // ─── Mouse tracking ───────────────────────────────────────────────────────
  wrap.addEventListener('mousemove', (e) => {
    const rect = wrap.getBoundingClientRect();
    mouse = { x: e.clientX - rect.left, y: e.clientY - rect.top };
  });
  wrap.addEventListener('mouseleave', () => { mouse = null; hoveredIdx = -1; });
  wrap.addEventListener('touchstart', () => { mouse = null; hoveredIdx = -1; }, { passive: true });

  const ro = new ResizeObserver(() => resize());
  ro.observe(wrap);
  resize();
  requestAnimationFrame(frame);
  requestAnimationFrame(() => { canvas.style.opacity = '1'; });

  const gridTex = wrap.querySelector('.grid-texture') as HTMLElement | null;
  if (gridTex) gridTex.style.display = 'none';

  // ─── FinalCTA: multi-platform dot grid, gentle pulse ─────────────────────
  {
    const ctaCard = document.querySelector('.final-cta') as HTMLElement | null;
    if (ctaCard) {
      const ctaCanvas = document.createElement('canvas');
      ctaCanvas.setAttribute('aria-hidden', 'true');
      ctaCanvas.style.cssText = 'position:absolute;inset:0;z-index:0;pointer-events:none;border-radius:inherit;opacity:0;transition:opacity 2s ease-in;';
      ctaCard.style.position = 'relative';
      ctaCard.insertBefore(ctaCanvas, ctaCard.firstChild);

      const cc = ctaCanvas.getContext('2d')!;
      interface CtaDot { gx: number; gy: number; pIdx: number; phase: number; }
      let ctaDots: CtaDot[] = [];
      let ctaDpr = 1;

      function buildCtaGrid() {
        ctaDots = [];
        const W = ctaCanvas.width / ctaDpr;
        const H = ctaCanvas.height / ctaDpr;
        const cols = Math.ceil(W / GRID) + 1;
        const rows = Math.ceil(H / GRID) + 1;
        for (let r = 0; r < rows; r++)
          for (let c = 0; c < cols; c++)
            ctaDots.push({ gx: c * GRID, gy: r * GRID, pIdx: pickPlatform(), phase: Math.random() * Math.PI * 2 });
      }

      function resizeCta() {
        ctaDpr = window.devicePixelRatio || 1;
        const rect = ctaCard!.getBoundingClientRect();
        const w = Math.round(rect.width), h = Math.round(rect.height);
        ctaCanvas.width  = w * ctaDpr;
        ctaCanvas.height = h * ctaDpr;
        ctaCanvas.style.width  = `${w}px`;
        ctaCanvas.style.height = `${h}px`;
        cc.setTransform(ctaDpr, 0, 0, ctaDpr, 0, 0);
        buildCtaGrid();
      }

      function frameCta() {
        requestAnimationFrame(frameCta);
        const W = ctaCanvas.width / ctaDpr;
        const H = ctaCanvas.height / ctaDpr;
        const t = performance.now() * 0.001;
        cc.clearRect(0, 0, W, H);
        for (const dot of ctaDots) {
          const [r, g, b] = PLATFORM_TMPLS[dot.pIdx].color;
          const alpha = 0.15 + Math.sin(t * 0.8 + dot.phase) * 0.06;
          cc.globalAlpha = alpha;
          cc.fillStyle   = `rgb(${r},${g},${b})`;
          cc.beginPath();
          cc.arc(dot.gx, dot.gy, 2, 0, Math.PI * 2);
          cc.fill();
        }
        cc.globalAlpha = 1;
      }

      const roCta = new ResizeObserver(() => resizeCta());
      roCta.observe(ctaCard);
      resizeCta();
      requestAnimationFrame(frameCta);
      requestAnimationFrame(() => { ctaCanvas.style.opacity = '1'; });

      const ctaGridTex = ctaCard.querySelector('.grid-texture') as HTMLElement | null;
      if (ctaGridTex) ctaGridTex.style.display = 'none';
    }
  }

  }); // end requestIdleCallback
</script>
