---
import Layout from '../layouts/Layout.astro';
import Header from '../components/layout/Header.astro';
import Footer from '../components/layout/Footer.astro';
import HeroPello from '../components/sections/HeroPello.astro';
import HowItWorks from '../components/sections/HowItWorks.astro';
import ProductSection from '../components/sections/ProductSection.astro';
import UseCases from '../components/sections/UseCases.astro';
import Stats from '../components/sections/Stats.astro';
import FAQ from '../components/sections/FAQ.astro';
import FinalCTA from '../components/sections/FinalCTA.astro';
---

<Layout title="Pello — AI-Powered Full Stack Marketing Agency" description="Pello propels business growth with AI-powered marketing across SEO, outreach, content, social, ads, and custom automations. Enter your website to see what Pello can do for you.">
  <Header />
  <main id="main-content">
    <div id="hero-wrap" class="relative">
      <canvas class="bg-canvas" aria-hidden="true" style="position:absolute;inset:0;z-index:0;pointer-events:none;will-change:transform;opacity:0;transition:opacity 2s ease-in;"></canvas>
      <HeroPello />
    </div>

    <HowItWorks />

    <div id="products">
      <ProductSection productIndex={0} flip={false} />
      <ProductSection productIndex={1} flip={true} />
      <ProductSection productIndex={2} flip={false} />
      <ProductSection productIndex={3} flip={true} />
      <ProductSection productIndex={4} flip={false} />
      <ProductSection productIndex={5} flip={true} />
      <ProductSection productIndex={6} flip={false} />
      <ProductSection productIndex={7} flip={true} />
    </div>

    <UseCases />
    <Stats />
    <FAQ />
    <FinalCTA />
  </main>
  <Footer />
</Layout>

<a id="skip-link" href="#main-content">Skip to main content</a>

<style is:global>
  #skip-link {
    position: absolute;
    top: -100%;
    left: 1rem;
    z-index: 9999;
    padding: 0.75rem 1.25rem;
    background: var(--color-brand-primary);
    color: white;
    font-weight: 600;
    border-radius: 0 0 0.5rem 0.5rem;
    text-decoration: none;
    transition: top 0.1s;
  }
  #skip-link:focus { top: 0; }
  #hero-wrap .url-input-wrap,
  .final-cta .url-input-wrap {
    background: rgba(255,255,255,0.06) !important;
    backdrop-filter: blur(24px) saturate(1.4);
    -webkit-backdrop-filter: blur(24px) saturate(1.4);
    border: 1px solid rgba(255,255,255,0.12) !important;
    box-shadow: 0 4px 30px rgba(0,0,0,0.2), inset 0 1px 0 rgba(255,255,255,0.05);
  }
  #hero-wrap .hero-panel {
    background: rgba(6,7,18,0.55) !important;
    backdrop-filter: blur(20px) saturate(1.3);
    -webkit-backdrop-filter: blur(20px) saturate(1.3);
    border: 1px solid rgba(255,255,255,0.12) !important;
  }
  #hero-wrap .panel-source-row { border-bottom: 1px solid rgba(255,255,255,0.1) !important; }
  #hero-wrap .core-inner {
    background: rgba(0,0,0,0.15) !important;
    border: 1px solid rgba(255,255,255,0.1) !important;
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
  }
  #hero-wrap .panel-core-section { border-bottom: 1px solid rgba(255,255,255,0.1) !important; }
  #hero-wrap .channel-node {
    background: rgba(255,255,255,0.03) !important;
    border: 1px solid rgba(255,255,255,0.1) !important;
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
  }
  #hero-wrap .panel-header-bar { border-bottom: 1px solid rgba(255,255,255,0.1) !important; }
  #hero-wrap .panel-ready-bar { border-top: 1px solid rgba(255,255,255,0.1) !important; }
  #hero-wrap .node-stat-text { color: #a78bfa !important; }
  #hero-wrap .hero-panel-mobile {
    background: rgba(10,10,20,0.3);
    backdrop-filter: blur(20px) saturate(1.3);
    -webkit-backdrop-filter: blur(20px) saturate(1.3);
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 1.25rem;
    padding: 1rem;
    box-shadow: 0 8px 32px rgba(0,0,0,0.3);
  }
  .final-cta {
    background: rgba(20,15,40,0.35) !important;
    backdrop-filter: blur(24px) saturate(1.4) !important;
    -webkit-backdrop-filter: blur(24px) saturate(1.4) !important;
    border-color: rgba(99,102,241,0.15) !important;
    box-shadow: 0 8px 40px rgba(0,0,0,0.3), inset 0 1px 0 rgba(255,255,255,0.04) !important;
    overflow: hidden;
    position: relative;
  }
  .final-cta > .absolute.inset-0.pointer-events-none { display: none !important; }
  #hero-wrap .grid-texture,
  #hero-wrap .absolute.inset-0.pointer-events-none[style*="radial-gradient"] { display: none !important; }
  #hero { background: transparent !important; }
</style>

<script>
const ric = (window as any).requestIdleCallback
  ? (window as any).requestIdleCallback
  : (cb: () => void) => setTimeout(cb, 1);

ric(() => {

function valueNoise(x: number): number {
  const i = Math.floor(x);
  const f = x - i;
  const u = f * f * (3 - 2 * f);
  const a = Math.sin(i * 127.1) * 43758.5453;
  const b = Math.sin((i + 1) * 127.1) * 43758.5453;
  return (a - Math.floor(a)) * (1 - u) + (b - Math.floor(b)) * u;
}

const COLORS: string[] = ['#6366f1','#8b5cf6','#a855f7','#c026d3','#7c3aed','#4f46e5','#9333ea','#6d28d9'];

interface FusionWave {
  yFrac: number;
  freq: number;
  baseAmp: number;
  phase: number;
  phaseSpeed: number;
  noiseOffset: number;
  noisePeriod: number;
  color: string;
}

const WAVES: FusionWave[] = COLORS.map((color, i) => ({
  yFrac: 0.1 + (i / 7) * 0.8,
  freq: 0.008 + i * 0.002,
  baseAmp: 10 + Math.sin(i) * 4,
  phase: i * 0.8,
  phaseSpeed: 0.006 + i * 0.001,
  noiseOffset: i * 93.7,
  noisePeriod: 3 + i * 0.3,
  color,
}));

// Cycle timing: 0-5s normal, 5-8s converge, 8-10s merged, 10-13s split, 13-18s normal, repeat at 18s
const CYCLE = 18;

let mouseY: number | null = null;
let mouseX: number | null = null;
let isHovering = false;
// Epoch start for cycle timing
let epochStart = performance.now() * 0.001;

function setupCanvas(canvas: HTMLCanvasElement, wrap: HTMLElement, waves: FusionWave[], isSimple: boolean): () => void {
  const ctx = canvas.getContext('2d')!;
  let W = 0, H = 0;
  let dpr = window.devicePixelRatio || 1;

  function resize() {
    dpr = window.devicePixelRatio || 1;
    const rect = wrap.getBoundingClientRect();
    W = Math.round(rect.width);
    H = Math.round(rect.height);
    canvas.width = W * dpr;
    canvas.height = H * dpr;
    canvas.style.width = W + 'px';
    canvas.style.height = H + 'px';
    ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
  }

  const ro = new ResizeObserver(() => resize());
  ro.observe(wrap);
  resize();

  const mobile = () => W < 768;

  function draw() {
    requestAnimationFrame(draw);
    if (W === 0 || H === 0) return;
    ctx.clearRect(0, 0, W, H);

    const now = performance.now() * 0.001;
    const speed = isHovering ? 2 : 1;
    // Advance epoch-based time faster when hovering
    const elapsed = (now - epochStart) * speed;
    const cycleT = elapsed % CYCLE;

    const SAMPLES = mobile() ? 100 : 200;

    // Determine phase of cycle
    // convergence target Y: center of canvas, or mouse Y if hovering
    const targetY = (!isSimple && mouseY !== null) ? mouseY : H * 0.5;

    let mergeT = 0; // 0 = fully separated, 1 = fully merged
    let phase: 'normal' | 'converging' | 'merged' | 'splitting' = 'normal';

    if (cycleT < 5) {
      phase = 'normal'; mergeT = 0;
    } else if (cycleT < 8) {
      phase = 'converging'; mergeT = (cycleT - 5) / 3;
    } else if (cycleT < 10) {
      phase = 'merged'; mergeT = 1;
    } else if (cycleT < 13) {
      phase = 'splitting'; mergeT = 1 - (cycleT - 10) / 3;
    } else {
      phase = 'normal'; mergeT = 0;
    }

    const isMerged = phase === 'merged';

    if (isMerged) {
      // Draw single composite wave
      const SAMPLES2 = mobile() ? 100 : 200;
      ctx.beginPath();
      for (let s = 0; s <= SAMPLES2; s++) {
        const x = (s / SAMPLES2) * W;
        let y = targetY;
        for (const w of waves) {
          const noiseVal = valueNoise(now / w.noisePeriod + w.noiseOffset);
          const amp = w.baseAmp * (0.6 + noiseVal * 0.8);
          y += Math.sin(x * w.freq + w.phase) * amp * 0.5;
        }
        if (s === 0) ctx.moveTo(x, y); else ctx.lineTo(x, y);
      }
      // Glow
      ctx.strokeStyle = '#ffffff';
      ctx.globalAlpha = 0.12;
      ctx.lineWidth = 6;
      ctx.stroke();
      ctx.strokeStyle = '#f5d87a';
      ctx.globalAlpha = 0.7;
      ctx.lineWidth = 1.5;
      ctx.stroke();
      ctx.globalAlpha = 1;

      // Labels
      ctx.font = 'bold 13px sans-serif';
      ctx.fillStyle = '#ffffff';
      ctx.globalAlpha = 0.8;
      ctx.textAlign = 'center';
      ctx.fillText('ONE PLATFORM', W * 0.35, H * 0.42);
      ctx.fillText('EVERY CHANNEL', W * 0.65, H * 0.58);
      ctx.globalAlpha = 1;
      ctx.textAlign = 'left';
    } else {
      // Draw individual waves lerped toward targetY
      for (let wi = 0; wi < waves.length; wi++) {
        const w = waves[wi];
        const baseY = w.yFrac * H;
        const currentY = baseY + (targetY - baseY) * mergeT;

        const noiseVal = valueNoise(now / w.noisePeriod + w.noiseOffset);
        const amp = w.baseAmp * (0.6 + noiseVal * 0.8);

        ctx.beginPath();
        for (let s = 0; s <= SAMPLES; s++) {
          const x = (s / SAMPLES) * W;
          const y = currentY + Math.sin(x * w.freq + w.phase) * amp;
          if (s === 0) ctx.moveTo(x, y); else ctx.lineTo(x, y);
        }
        const alphaMult = isMerged ? 0 : (mergeT > 0 ? 0.5 : 1);
        ctx.strokeStyle = w.color;
        ctx.globalAlpha = 0.4 * alphaMult;
        ctx.lineWidth = 0.8 + mergeT * 0.5;
        ctx.stroke();
        ctx.globalAlpha = 1;
      }
    }

    // Scan line
    if (!isSimple && !isMerged) {
      const scanY2 = ((now % 8) / 8) * H;
      const sg = ctx.createLinearGradient(0, scanY2 - 10, 0, scanY2 + 10);
      sg.addColorStop(0, 'rgba(162,52,250,0)');
      sg.addColorStop(0.5, 'rgba(162,52,250,0.05)');
      sg.addColorStop(1, 'rgba(162,52,250,0)');
      ctx.fillStyle = sg;
      ctx.fillRect(0, scanY2 - 10, W, 20);
    }

    for (const w of waves) w.phase += w.phaseSpeed * (isHovering ? 2 : 1);
  }

  requestAnimationFrame(draw);
  requestAnimationFrame(() => { canvas.style.opacity = '1'; });
  return () => ro.disconnect();
}

const heroWrap = document.getElementById('hero-wrap')!;
const heroCanvas = heroWrap.querySelector<HTMLCanvasElement>('.bg-canvas')!;
setupCanvas(heroCanvas, heroWrap, WAVES, false);

heroWrap.addEventListener('mousemove', (e: MouseEvent) => {
  const rect = heroWrap.getBoundingClientRect();
  mouseY = e.clientY - rect.top;
  mouseX = e.clientX - rect.left;
  if (!isHovering) { isHovering = true; epochStart = performance.now() * 0.001 - ((performance.now() * 0.001 - epochStart) * 0.5); }
});
heroWrap.addEventListener('mouseleave', () => { mouseY = null; mouseX = null; isHovering = false; });

const gridTex = heroWrap.querySelector<HTMLElement>('.grid-texture');
if (gridTex) gridTex.style.display = 'none';

const ctaCard = document.querySelector<HTMLElement>('.final-cta');
if (ctaCard) {
  const ctaCanvas = document.createElement('canvas');
  ctaCanvas.setAttribute('aria-hidden', 'true');
  ctaCanvas.style.cssText = 'position:absolute;inset:0;z-index:0;pointer-events:none;border-radius:inherit;opacity:0;transition:opacity 2s ease-in;';
  ctaCard.style.position = 'relative';
  ctaCard.insertBefore(ctaCanvas, ctaCard.firstChild);
  const ctaWaves: FusionWave[] = [
    { yFrac: 0.3, freq: 0.012, baseAmp: 9, phase: 0, phaseSpeed: 0.008, noiseOffset: 200, noisePeriod: 4, color: '#6366f1' },
    { yFrac: 0.5, freq: 0.015, baseAmp: 11, phase: 1.5, phaseSpeed: 0.01, noiseOffset: 350, noisePeriod: 3.5, color: '#a855f7' },
    { yFrac: 0.7, freq: 0.01, baseAmp: 8, phase: 3.0, phaseSpeed: 0.007, noiseOffset: 500, noisePeriod: 5, color: '#9333ea' },
  ];
  setupCanvas(ctaCanvas, ctaCard, ctaWaves, true);
  const gt = ctaCard.querySelector<HTMLElement>('.grid-texture');
  if (gt) gt.style.display = 'none';
}

}); // end ric
</script>
